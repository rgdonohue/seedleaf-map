{
  "name": "seedleaf-map",
  "version": "1.0.0",
  "description": "",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "check": "node scripts/check-data.js",
    "prebuild": "npm run check && rm -rf dist .parcel-cache",
    "build": "parcel build --public-url ./",
    "deploy": "npm run build && cp src/CNAME dist/ && surge dist"
  },
  "author": "rgdonohue",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.8.3",
    "process": "^0.11.10"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.4.0",
    "leaflet": "^1.9.3"
  }
}
